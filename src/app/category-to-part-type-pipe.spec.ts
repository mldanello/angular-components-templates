import { CategoryToPartTypePipe } from './category-to-part-type-pipe';

describe('CategoryToPartTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryToPartTypePipe();
    expect(pipe).toBeTruthy();
  });
});
